package com.example.memorycat
/*

import TodayWordViewModel
import android.util.Log
import android.view.View
import android.view.ViewGroup
import androidx.fragment.app.viewModels

val todayWordViewModel: TodayWordViewModel by viewModels()

public class getTodayWord(idx: Int) {
    val todayWordNames = todayWordViewModel.todayWordNames.value
    val firstTenWords = todayWordNames?.take(10)?.toList()
    val word = firstTenWords?.getOrElse(idx) { "" }
    //val word = firstTenWords[idx]
}

 */

